{
  "name": "channieinifigge-website-tooling",
  "private": true,
  "scripts": {
    "check": "node --check main.js",
    "commitlint": "commitlint",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2"
  }
}
